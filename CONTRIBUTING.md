# Contributing

Small, testable changes are preferred.

Before opening a pull request:

```bash
npm test
npm run check
```

For bug fixes, include a minimal reproduction. For behavior changes, add or
update a test. Please avoid adding a runtime dependency when the standard
library can do the job clearly.
